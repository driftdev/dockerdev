services:
  kvrocks:
    image: apache/kvrocks:latest
    container_name: kvrocks
    restart: always
    environment:
      KVROCKS_ADMIN_SECRET: dev
    ports:
      - 6666:6666
    volumes:
      - data:/var/lib/kvrocks

volumes:
  data:
