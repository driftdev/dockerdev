services:
  typesense:
    image: typesense/typesense:26.0
    container_name: typesense
    restart: always
    command: --data-dir /data --api-key=dev --enable-cors
    ports:
      - 8108:8108
    volumes:
      - data:/data

volumes:
  data:
